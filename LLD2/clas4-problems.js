/**
 * 
 * 1. celcius to Fahrenheit
 * eg: convertTemperature(100, "C") → 212F.
 * 
 * function CelToFah(celsius) {
  return celsius * 9 / 5 + 32;
}

*
function FahToCel(fahrenheit) {
  return (fahrenheit - 32) * 5 / 9;
}

 * function convertTemperature(temp, unit) {
  // Write code here

}

Test cases:
console.log(convertTemperature(100, "C")); // "212F"
console.log(convertTemperature(32, "F"));  // "0C"


 * 
*/

/**
 *
 * Q2: Reversing the words in sentence
 * he text -> Hello world    O/P" world Hello
 *
 * reverseWord(sentence){
 * }
 *
 * aba
 * madam
 *
 *
 *
 */

/**
 * for loop
 * data[0] -> {name:Alice, score:92} ; data[0].name
 * const data = [
 * {name:"Alice",score:92},
 * {name:"Bob",score:80},
 * {name:"Charlie",score:60},
 * {name:"Nitin",score:95}
 * ]
 *
 * {
 * A: ["Alice","Nitin"] > 90
 * B: ["Bob"], > 75 and less than 90
 * C:["Charlie"] <= 60
 * }
 *
 * function categorizeStudents(studentList){
 * // logic
 * return categories
 * }
 */

/**
 *
 * Mini lirary management system
 * - list all books
 * - borrow a book
 * - return a book
 * - filter books by specific author
 *
 * {
 *
 * title:"Book Name",
 * author:"Author Name",
 * isAvailable:true
 * }
 *
 */

/**
 *
 * secretMessage(secret)
 *
 * const secret = secretmessage("Hidden Code")
 * console.log(secret()) // Hidden code
 *
 * function secretMessage(secret){
 * // logic
 * }
 */

/**
 *
 * limitCalls(fn,n) // n =2
 */
